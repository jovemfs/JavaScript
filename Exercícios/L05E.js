let contadora = 0;

while (contadora < 20) {

    if ((contadora % 2) != 0) {
        console.log(contadora + " é ímpar.");
    } else { }

    contadora++
}

alert("Veja os valores ímpares de 1-20 em inspecionar");