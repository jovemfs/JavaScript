let soma, media, contadora;

contadora = 50;
while (contadora < 70) {
    if ((contadora % 2) == 0) {
        soma = contadora + contadora;
    }
    contadora++
}

media = soma / 20

alert("Soma de 50 a 70: " + soma + ". Média: " + media);