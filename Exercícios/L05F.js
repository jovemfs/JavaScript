let contadora = 0;
let divisor = 0;

while (contadora < 201) {

    if ((contadora % 4) == 0) {
        console.log(contadora + " é dívisivel por 4.");
        console.log(contadora + "/4 = " + (contadora / 4));
        console.log("");
    }

    contadora++
}

alert("Veja os valores divisíveis por 4 em inspecionar.")